import Upload, { UploadProps, UploadFile, UploadFileStatus } from './upload';
import Dragger from './dragger';

export type { UploadProps, UploadFile, UploadFileStatus };
export { Dragger };
export default Upload;
